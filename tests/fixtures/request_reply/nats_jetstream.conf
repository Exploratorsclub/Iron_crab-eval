# Minimal NATS config with JetStream for request-reply E2E harness.
# Port wird zur Laufzeit durch den Harness prepended (temp file).
# Template: Harness schreibt "port: <PORT>\n" + diesen Inhalt.
jetstream {
  max_mem: 64M
  max_file: 64M
}
